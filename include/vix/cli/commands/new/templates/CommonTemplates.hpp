#pragma once

/**
 * @file CommonTemplates.hpp
 * @author Gaspard Kirira
 *
 * Shared file-content helpers for `vix new` templates.
 */

#include <string>

namespace vix::commands::new_cmd::templates
{

  std::string make_gitkeep();

} // namespace vix::commands::new_cmd::templates
